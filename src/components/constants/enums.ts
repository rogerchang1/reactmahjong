export enum BlockType {
  UNKNOWN,
  PON,
  CHI,
  OPENKAN,
  CLOSEDKAN,
}

export enum Agari {
  TSUMO,
  RON,
}

export enum Wind {
  EAST,
  SOUTH,
  WEST,
  NORTH,
}

export enum RiichiSetting {
  NONE,
  RIICHI,
  DOUBLERIICHI,
}

export enum SpecialYakuSetting {
  NONE,
  CHANKAN,
  RINSHAN,
  HAITEI,
  HOUTEI,
}